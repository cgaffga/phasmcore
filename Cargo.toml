[package]
name = "phasm-core"
version = "0.1.0"
edition = "2024"

[features]
default = []
parallel = ["rayon"]

[dependencies]
aes-gcm-siv = "0.11"
argon2 = "0.5"
rand = "0.8"
rand_chacha = "0.3"
crc32fast = "1.4"
num-complex = "0.4"
rayon = { version = "1", optional = true }
